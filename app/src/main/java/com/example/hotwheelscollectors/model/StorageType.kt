package com.example.hotwheelscollectors.model

/**
 * Enum representing different storage options for user's collection
 */
enum class StorageType {
    LOCAL,
    GOOGLE_DRIVE,
    DROPBOX,
    ONEDRIVE
}
